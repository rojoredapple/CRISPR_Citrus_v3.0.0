<%= form_with(model: gene_athal) do |form| %>
  <% if gene_athal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gene_athal.errors.count, "error") %> prohibited this gene_athal from being saved:</h2>

      <ul>
        <% gene_athal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Gene_ID %>
    <%= form.text_field :Gene_ID %>
  </div>

  <div class="field">
    <%= form.label :Orthogroup_genes_Athal %>
    <%= form.text_field :Orthogroup_genes_Athal %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
