<%= form_with(model: gene_study) do |form| %>
  <% if gene_study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gene_study.errors.count, "error") %> prohibited this gene_study from being saved:</h2>

      <ul>
        <% gene_study.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Gene_ID %>
    <%= form.text_field :Gene_ID %>
  </div>

  <div class="field">
    <%= form.label :Study %>
    <%= form.text_field :Study %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
