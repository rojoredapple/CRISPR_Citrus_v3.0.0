<%= form_with(model: gp_note) do |form| %>
  <% if gp_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gp_note.errors.count, "error") %> prohibited this gp_note from being saved:</h2>

      <ul>
        <% gp_note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Gene_ID %>
    <%= form.text_field :Gene_ID %>
  </div>

  <div class="field">
    <%= form.label :Note %>
    <%= form.text_field :Note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
