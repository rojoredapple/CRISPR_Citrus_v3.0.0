<%= form_with(model: plant_note) do |form| %>
  <% if plant_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plant_note.errors.count, "error") %> prohibited this plant_note from being saved:</h2>

      <ul>
        <% plant_note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Plant_ID %>
    <%= form.text_field :Plant_ID %>
  </div>

  <div class="field">
    <%= form.label :Note %>
    <%= form.text_field :Note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
