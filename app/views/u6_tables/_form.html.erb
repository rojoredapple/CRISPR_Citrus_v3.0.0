<%= form_with(model: u6_table) do |form| %>
  <% if u6_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(u6_table.errors.count, "error") %> prohibited this u6_table from being saved:</h2>

      <ul>
        <% u6_table.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Vector_ID %>
    <%= form.text_field :Vector_ID %>
  </div>

  <div class="field">
    <%= form.label :sgRNA_ID %>
    <%= form.text_field :sgRNA_ID %>
  </div>

  <div class="field">
    <%= form.label :Position %>
    <%= form.text_field :Position %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
